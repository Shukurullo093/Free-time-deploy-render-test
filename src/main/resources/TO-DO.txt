    -upload user image
    -view image
    save phone
    update user info
    -create user & user image entity
    -register
    -email verification
    -login
    -test api with postman
    -security & jwt
    -test api with ngrok & postman

    -create contacts entity, repository
    invite friend

    DateFormat dateFormat = new SimpleDateFormat("dd/MM/yyyy");
    messageDto.setDate(dateFormat.format(homeMessage.getCreatedDate()));